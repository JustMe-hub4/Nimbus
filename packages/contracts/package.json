{
  "name": "@nimbus/contracts",
  "version": "1.0.0",
  "description": "Shared contracts between Nimbus services",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  }
}
