{
  "name": "@nimbus/shared-config",
  "version": "1.0.0",
  "description": "Shared configuration for Nimbus services",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  }
}
